메서드 호출과 값 전달1

**자바는 항상 변수의 값을 복사해서 대입한다!!** ⇒ 이 대원칙을 항상 이해할 것

/*
int num2 = num1;
int num2 = num1; // num2 변수에 대입하기 전 num1의 값인 5를 읽고, 복사함
num2 = 10; // num2에 10을 대입
*/
⇒ num1의 값을 읽어도 num1에 있는 기존 값이 유지되고, 새로운 값(복사된 값 = 5)이 num2에 들어간다.
