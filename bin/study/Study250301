*패키지 규칙
1. 패키지의 이름과 위치는 폴더 디렉토리 위치와 같아야 함
2. 패키지 이름은 모두 소문자 사용
3. 패키지 이름의 앞 부분에는 일반적으로 회사의 도메인 이름을 거꾸로 사용함 (관례)
	ex) com.company.myapp 

*패키지가 계층 구조를 이루더라도 모든 패키지는 서로 다른 패키지이다!
-> 서로 다른 패키지 클래스에서 다른 패키지 클래스가 필요하면 import를 해서 사용하는 수 밖에 없다.

