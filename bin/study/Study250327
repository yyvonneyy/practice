*super
- 부모와 자식의 필드명이 같거나 메서드가 오버라이딩 되어 있으면, 자식에서 부모를 호출할 수 없음
- 이 때, super 키워드를 사용하여 부모를 참조

*상속 관계의 인스턴스를 생성하면 결국 메모리 내부에는 자식과 부모 클래스가 각각 생성됨
-> 따라서 각각의 생성자도 모두 호출 되어야 함

** 상속 관계를 사용하면 자식 클래스의 생성자에서 부모 클래스의 생성자를 반드시 호출해야 함!

*자식 클래스에서 여러 메소드가 있으면 그 중 하나의 메소드 내에서 반드시 한번은 super() 생성자를 호출해야 함
- 단 기본 생성자인 경우 생략 가능

- 상속 관계의 생성자 호출은 결과적으로 부모 -> 자식 순서로 실행하게 됨
- 따라서 부모의 데이터를 먼저 초기화하고 그 다음에 자식의 데이터를 초기화 함
