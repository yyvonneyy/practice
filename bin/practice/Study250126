메서드 정의
1. 제어자 : public, static 같은 부분 
2. 반환 타입 (Return type) : 메서드가 실행 된 후 반환하는 데이터의 타입 / 값을 반환하지 않는 경우 없다는 뜻의 void를 사용
3. 메서드 이름 : 메서드의 이름 -> 호출 시 사용
4. 매개변수 : 입력 값 -> 메서드 내에서 사용할 수 있는 변수 (옵션)
5. 메서드 본문 : 실제 메서드의 코드가 위치 / {}중괄호 사이에 코드 작성

*매개변수가 없는 경우
- 선언 : 매개변수를 비워두고 선언 ex) public static void add();
- 호출 : 인수를 비워두고 호출 ex) add();

*반환타입이 없는 경우
- 선언 : void 로 선언
- 호출 : printHeader(); 와 같이 반환 타입이 없으므로 메서드만 호출하고 반환 값을 받지 않으면 됨  

*void와 return 생략
- 모든 메서드는 항상 return 을 호출해야함 -> return을 만나면 해당 메서드는 종료됨
- 하지만 반환 타입이 void인 경우에는 예외로 return 생략이 가능

*반환 타입이 있으면 반드시 값을 return 해야함 

*메서드를 호출하고 반환 값을 사용하지 않는 경우는 그냥 반환 값을 무시해도 됨 