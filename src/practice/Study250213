*배열의 도입
- 자바에서의 대입은 항상 변수에 들어 있는 값을 복사한다.

Student student1 = new Student();
Student student2 = new Student();

Students[] students;
students[0] = student1;
students[1] = student2;

- 이 경우 배열에는 인스턴스 자체가 들어있는 것이 아닌 인스턴스의 위치를 가리키는 참조값이 들어있을 뿐

*배열에 들어있는 객체 사용
- 베열에 들어있는 객체를 사용하려면 먼저 배열에 접근하고, 그 다음에 객체에 접근하면 된다

